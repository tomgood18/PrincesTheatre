import { StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginTop:50,
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 40
    },
    titleText: {
        fontSize: 30,
        fontWeight: "bold"
    },
    subText: {
        fontSize: 20
    },
    descText: {
        textAlign: 'center'
    }
  });

  export {styles}
